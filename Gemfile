source "https://rubygems.org"

# use awesomeprint to easily debug
gem "awesome_print"

# use Google cloud language to make sentiment analysis
gem "google-cloud-language"

# use rufus-scheduler to time when emails are sent and processed
gem "rufus-scheduler"
